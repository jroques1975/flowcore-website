services:
  web:
    image: nginx:alpine
    container_name: flowcore-website
    restart: unless-stopped
    ports:
      - "15123:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
